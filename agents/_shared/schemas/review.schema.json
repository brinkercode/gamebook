{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "department review verdict",
  "description": "Craft review by a department director/reviewer (eng-director, art-director, design-director). Blockers fail the wave; advisories don't. Findings should be adversarially verified before reaching a report the user sees.",
  "type": "object",
  "required": ["result", "blockers"],
  "properties": {
    "result": { "type": "string", "enum": ["pass", "fail"] },
    "blockers": { "type": "array", "items": { "type": "string" }, "description": "'file:line — what — fix' format" },
    "advisories": { "type": "array", "items": { "type": "string" } },
    "dimensions_checked": { "type": "array", "items": { "type": "string" }, "description": "e.g. gas-patterns, bp-hygiene, replication, perf-antipatterns, naming, save-integrity" },
    "docs_drift": { "type": "array", "items": { "type": "string" } }
  }
}
