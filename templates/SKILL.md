---
name: {{SKILL_NAME}}
description: {{TRIGGER_DESCRIPTION}}
version: "1.0.0"
---

# {{SKILL_TITLE}}

> {{ONE_LINE_PURPOSE}}

## When to use

Invoke when {{TRIGGER_CONDITIONS}}. Skip when {{SKIP_CONDITIONS}}.

## How it works

Keep this section short — it is always loaded. Push depth into `resources/` and only
read those files when the step calls for them (progressive disclosure).

1. **{{STEP_1}}** — {{STEP_1_DETAIL}}
2. **{{STEP_2}}** — {{STEP_2_DETAIL}}
3. **{{STEP_3}}** — {{STEP_3_DETAIL}}

## UE5 context

State which UE5 subsystems, GAS surfaces, or Content paths this skill touches:
- Modules affected: _(e.g., `Source/{{PROJECT_NAME}}/`, `Plugins/`)_
- Asset paths: _(e.g., `Content/{{CONTENT_DIR}}/`)_
- Config files: _(e.g., `Config/DefaultGame.ini`)_

## Resources (read on demand)

- `resources/{{RESOURCE_1}}` — {{WHEN_TO_READ_1}}
- `resources/{{RESOURCE_2}}` — {{WHEN_TO_READ_2}}

## Output

{{WHAT_THE_SKILL_PRODUCES}} — and where it goes.
